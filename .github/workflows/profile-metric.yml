name: Update Profile Metric Badge

on:
  schedule:
    - cron: "17 */6 * * *"
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/profile-metric.yml"
      - "scripts/update-profile-metric.mjs"

permissions:
  contents: write

jobs:
  update-profile-metric:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Generate profile metric badge
        run: node scripts/update-profile-metric.mjs
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}

      - name: Commit badge updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/profile-visitors.svg
          if git diff --cached --quiet; then
            echo "No badge changes to commit."
            exit 0
          fi
          git commit -m "chore: update profile metric badge"
          git push
